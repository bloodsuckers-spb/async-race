import Details from '../../../Details';
import bestTimeSummary from '../BestTimeSummary';
import bestTimeList from '../BeatTimeList';

const BestTimeDetails = new Details(bestTimeSummary, bestTimeList);

export default BestTimeDetails;
