import Details from '../../../Details';
import winsSummary from '../WinsSummary';
import winsList from '../WinsList';

const bestTimeSelect = new Details(winsSummary, winsList);

export default bestTimeSelect;
